node_modules/
/dist/
/icons/

# Anchored to the repo root on purpose: an unanchored `screenshots/` also matches
# scripts/screenshots/, which holds the capture script and must be linted.
/screenshots/
